import React from 'react';

const Login: React.FC = () => {
    return (
        <div>
            <h1>Login to Colosseum</h1>
            <button>Sign in with Google</button>
        </div>
    );
};

export default Login;
